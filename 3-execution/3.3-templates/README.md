# 3.3 — Templates

> **Stub — to be built (Tier 1).** Source: the Huisraad templates (777 nodes `gittpl.SKILL-TEMPLATE.md`,
> `gittpl.RULEBOOK-TEMPLATE.md`, `gittpl.AI-STATE-TEMPLATE.md`) + a registry template derived from the
> registry discipline. Translate to English, strip all use-case content, keep the format and the inline
> guidance.

## What this folder must contain (one file each)
- **`SKILL-TEMPLATE.md`** — the skill format (YAML frontmatter + sections + the `description` guidance).
- **`RULEBOOK-TEMPLATE.md`** — the self-learning format (dated lines; hard vs global vs per-context rules).
- **`STATE-TEMPLATE.md`** — the state/`ai-state` format (the root vs project profiles; header block;
  recency rule; layered changelog).
- **`REGISTRY-TEMPLATE.md`** — the what/where/status index format.

> These are the copy-and-fill blanks. Keep the guidance comments; ship no filled-in instance.
