# RULEBOOK — <skill/process name>

> Read this at the start of every run and apply the rules. After every correction (or any gap between what you
> did and what it should have been) add a rule here. Short, concrete, dated. This is the self-learning memory:
> nothing is lost.

## How to update this file
- A new learning = one bullet: `- [YYYY-MM-DD] <rule> (re: <short trigger>)`.
- A contradicting older rule? Update it or strike it through — no duplicates.
- Only real, reusable lessons. No one-off facts.
- Hard rules (marked as such) are never changed without explicit sign-off.

## Hard rules (non-overridable)
- [EXAMPLE] Never send to others directly, only draft. Wait for an explicit "send it."

## Global rules
- (empty — fill in during use)

## Per-context rules
- (empty — fill in during use)

---
*Use this format for every skill/process so learnings are captured the same way everywhere.*
