# RULEBOOK — example-draft-reply

> **GOLDEN EXAMPLE.** Read at the start of every run and apply. After every correction, add a dated rule here.
> This is the self-learning memory: nothing is lost. (The lines below are illustrative.)

## How to update this file
- A new learning = one bullet: `- [YYYY-MM-DD] <rule> (re: <short trigger>)`.
- A contradicting older rule? Update it or strike it through — no duplicates.
- Only real, reusable lessons. No one-off facts.
- Hard rules are never changed without explicit sign-off.

## Hard rules (non-overridable)
- [EXAMPLE] Never send. Only draft. Wait for an explicit "send it."

## Global rules
- [EXAMPLE-2026-01-01] Always answer the question *behind* the question before answering the literal one (re:
  a reply that gave a format when the sender really wanted a recommendation).

## Per-context rules
- (empty — fill in during use)
