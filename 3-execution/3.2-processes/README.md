# 3.2 — Processes

> **Stub — to be built (Tier 1 format / Tier 2 implementation).** Source: the process format + the README
> (`gitproc.README.md`) and the skill-vs-process test from `SYSTEM-PROTOCOL.md`. Ship the **format + the
> test + one anonymized golden example**. Do NOT ship Huisraad's concrete automations (comms-triage,
> mail-push, etc. — those are personal/use-case content).

## What this folder must contain
- **What a process is:** a skill that runs *automatically* (runner/schedule/event) = skill + rulebook +
  self-learning + a runner, plus a health-check and a ping-on-failure.
- **The skill-vs-process test:** runner starts it without a human → process; must be called → skill.
- **One golden example:** a fully anonymized process described as a pattern (trigger → action → where output
  goes), concept-first — *not* real scripts or configs.
