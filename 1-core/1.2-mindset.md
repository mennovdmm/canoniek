# 1.2 — The mindset (the constitution template)

> **Stub — to be built (Tier 1, excellent).** Source: `core` / `commands/core.md` (777 node `l1.core`).
> Extract verbatim-first, translate to English. The *framework and rules* are generic → ship. The *filled-in
> identity* (person, company, team, clients, mail addresses, drive structure, specific tools) →
> bootstrap-question / placeholder (see [1.3](1.3-identity-placeholders.md)).

## What this doc must cover (the generic constitution)
- **Why the system exists:** a self-improving system — *always be learning*; logged feedback = investment,
  unlogged = waste.
- **The hard rules:** outbound messages are always drafts (a human sends); RAG-first / "you know nothing"
  (search and cite before advising); no secrets in version control; non-destructive (archive, never delete;
  `.bak`, never overwrite); log what you did.
- **Way of working:** orient first (build *current* understanding, weigh every source by date); default =
  act, not ask (with the narrow list of when to ask first); how you ask (a popup, never a trailing
  plain-text question); step-by-step with sign-off for system/architecture/migration work.
- **The orientation sequence** and the layered logging discipline (detail down, roll-up up).

> Replace every identity-specific block with an explicit placeholder/bootstrap question. Strip the full
> Huisraad strip-list (names, clients, hosts, IPs, personal processes).
